// build.gradle configures project dependencies and build tasks.

apply plugin: 'java-library'

group = 'com.tsl'
archivesBaseName = 'TurnGenerator'
version = '0.0.1-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}